> [!tip] # Representação Polar
> Seja $z = a + bi \in \mathbb{C}$ , com $z \neq 0$. Então o ponto $P(a,b)$ é diferente de $0(0,0)$ .

Seja $\theta_0$ a medida do ãngulo que o eixo ral positico forma com o segmento de reta OP, então $$a = |z| \cdot cos(\theta_0) \text{ e } b = |z| \cdot $$Assim,$$ z = |z| \cdot (cos(\theta_o) + i \cdot  sen(\theta_o))$$
Note que se $\theta = \theta_0 + 2 \cdot K \cdot \pi$, onde $k \in \mathbb{Z}$, então $$z = |z| \cdot (cos(\theta) + i \cdot sen(\theta))$$ Assim, podemos representar $z$ na forma $$z = |z| \cdot (cos(\theta) + i \cdot sen(\theta))) \text{ , } \theta \in \mathbb{R}$$ tal que representação é chamada de **Representação polar** de $z$ e o ângulo $\theta$ é chamado de argumento de $z$ ![[Pasted image 20251119093540.png]]
$$cos(\theta) = cos(\theta_0) = \frac{a}{|z|} \rightarrow a = |z| \cdot cos(\theta_0)$$$$ sen(\theta) = sen(\theta_0) = \frac{b}{|z|} \rightarrow b = |z| \cdot sen(\theta_0)$$
Por outro lado, se $\theta \in \mathbb{R}$ satisfaz $x$, então $$cos(\theta) = cons(\theta_0) \text{ e } sen(\theta) = sen(\theta_0)$$Logo, $\theta = \theta_0 + z \cdot K \cdot \pi$, para lgum $k \in \mathbb{Z}$. Assim, $\theta$ éo argumento de $z$ se, e somente se $$|theta = |theta + 2 \cdot K \cdot \pi \text{ , } K \in \mathbb{Z}$$
